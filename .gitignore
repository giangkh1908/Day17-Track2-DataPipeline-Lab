.venv*/
__pycache__/
*.pyc
warehouse.duckdb
quarantine.csv
dbt_project/dbt.duckdb
dbt_project/target/
dbt_project/logs/
data/_clean.parquet

# derived AI datasets (regenerated by flywheel.py) — keep the submitted jsonl
datasets/*
!datasets/*.jsonl
.pytest_cache/

# opencode config (skills, agents — local tooling, not part of the lab)
.opencode/

# stray student-simulation copies (never part of the lab)
student-*/
verify-*/
